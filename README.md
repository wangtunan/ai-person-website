# AI Person Website

Next.js + React + Tailwind CSS + Vercel starter project.

## 本地开发

```bash
pnpm install
pnpm dev
```

## 常用命令

```bash
pnpm lint
pnpm build
pnpm start
```

## Vercel 部署

把仓库导入 Vercel 后，Vercel 会自动识别 Next.js 项目。当前 `vercel.json` 只声明框架类型：

- Framework: `nextjs`
